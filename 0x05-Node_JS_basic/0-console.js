/**
 * create a function named displayMessage that prints 
 * STDOUT the string argument
 * @param {string} msg the message 
 */

const displayMessage = (msg) => {
    console.log(msg);

};

module.exports = displayMessage;